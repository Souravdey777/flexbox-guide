import React from 'react';

function FlexCode(props) {
    return (
        <div>
            {
                `. Cotainer {
                    display : flex,
                    
                }`
            }
        </div>
    );
}

export default FlexCode;